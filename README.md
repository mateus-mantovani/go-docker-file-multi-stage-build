# Go lang Dockerfile
This is an example of how to build a small image to run go.

## How to build?
To build the image use the following command
```bash
docker build -t mateus21/codeeducation:latest .
```

## Pull and execute image from Docker Hub
This image is already in docker hub. You can pull and execute it with the following command:
```bash
docker run mateus21/codeeducation
```